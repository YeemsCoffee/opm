# MNQ overlay — apply on top of config/default.yaml:
#   fvg-backtest run --symbol MNQ --config config/mnq.yaml
instrument: MNQ
contract_mode: CONTINUOUS
data:
  provider: databento
execution:
  mode: ONE_MINUTE_CONSERVATIVE
