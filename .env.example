# Databento API credential used by the databento provider.
# Get one at https://databento.com (Historical API access to GLBX.MDP3).
DATABENTO_API_KEY=db-your-key-here

# Optional overrides (defaults shown)
#FVG_DATA_CACHE=data_cache
#FVG_RUNS_DIR=runs
