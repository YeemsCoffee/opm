{
  "_comment": "Defines the path the sync script takes through Homebase and how it recognizes the data on each page. Edit this file (not the code) when Homebase's layout changes or these placeholder keywords need calibrating against the real site.",
  "base_url": "https://app.joinhomebase.com",
  "login_url": "https://app.joinhomebase.com/login",
  "hours_report": {
    "_comment": "Confirmed against a real Timesheets screenshot: columns are Team member / Worked / Total paid breaks / Total unpaid breaks / Issues / Actions. The total is embedded in the Worked cell as 'Total: X hrs Y min' — extracted by regex, not a dedicated hours column. Prefer the page's own 'Pay period' toggle over summing daily views once its URL/param is confirmed.",
    "path": "/timesheets",
    "name_header_keywords": ["team member", "name", "employee"],
    "worked_header_keywords": ["worked"]
  },
  "trade_board": {
    "_comment": "PLACEHOLDER — confirmed WRONG against a real schedule screenshot. There is no separate trade-board table: a covered shift shows as a flagged cell (orange, warning icon, 'Open Shift approved') directly in the regular weekly Schedule Builder grid, under the covering employee's own row. This section's path/table-column approach needs a rewrite once it's decided whether grid-only (who covered it, already visible from the row) is enough, or whether click-through detail (who originally released it) is also needed. Sync failures here are isolated from the hours sync and never block it.",
    "path": "/schedule_builder/trades",
    "date_header_keywords": ["date", "shift date"],
    "released_by_header_keywords": ["released by", "original", "posted by", "dropped by"],
    "covered_by_header_keywords": ["covered by", "picked up by", "claimed by", "assigned to", "traded to"],
    "role_header_keywords": ["role", "position"],
    "status_header_keywords": ["status"]
  }
}
